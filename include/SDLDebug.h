#include <SDL2/SDL.h>

/**
 * Draws a vector based on the given viewport information, with pretty arrow
*/
void drawVector(SDL_Renderer* rend, Vector<float, 2> vec, Vector<float, 2> origin,
                float camx, float camy, float zoom) {
    // NYI
}